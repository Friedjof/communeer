# pnpm's default supply-chain cooldown rejects packages published within the
# last ~24h; our lockfile is pinned/reviewed, so disable it rather than have
# every fresh Docker build randomly fail depending on when it happens to run.
minimum-release-age=0
